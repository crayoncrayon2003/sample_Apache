# about
Apache Mahout is a distributed linear-algebra / machine-learning library (no database).
This sample uses the in-core `mahout-math` primitives to compute column means via
matrix algebra and summary statistics (mean, sd, quartiles).

# build and run
```bash
mvn -q compile exec:java
```
