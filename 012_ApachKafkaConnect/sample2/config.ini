[DEFAULT]
# Used by host-side scripts to reach the Kafka Connect REST API (:8083)
HOST_IP = localhost
# Docker bridge gateway of fiware-network. Used by connectors (inside the
# kafka-connect container) to reach the HTTP servers running on the host.
DOCKER_HOST_IP = 172.19.1.1
