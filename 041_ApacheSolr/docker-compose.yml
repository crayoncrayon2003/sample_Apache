version: '3.8'

services:
  solr:
    image: solr:latest
    container_name: solr
    ports:
      - "8983:8983"
    command:
      - solr-precreate
      - sensor
    volumes:
      - solr_data:/var/solr
    restart: unless-stopped

volumes:
  solr_data: {}
